dcl num NumTest to 5 /*Here har vi vores global var */
dcl bool BoolTest
dcl text TextTest
dcl someClass ClassTest
                     /*du kan ogsaa lave c
Main begin 
   dcl num NumTest to 5 
   dcl bool BoolTest
   dcl text TextTest
   dcl someClass ClassTest
end

class SomeClass begin
   dcl num NumTest to 5 
   dcl bool BoolTest
   dcl text TextTest
   dcl someClass ClassTest

   dcl func x() begin
      dcl num NumTest to 5 
      dcl bool BoolTest
      dcl text TextTest
      dcl someClass ClassTest

      set numTest to 5
      set BoolTest to true
      set TextTest to "test"
      set ClassTest to new someClass()

      if point.y > ball.point.y then /* hvorfor virker dette */
      end
      if some > somde then
      end

      for i upto something do
         for i upto something do
            if point.y > ball.point.y then /* hvorfor virker dette */
            end
         end
      end
      
      while i < something do
         while true do
            if something = somethingelse then
            else
            end
         end
      end
   end
end