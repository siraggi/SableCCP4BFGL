dcl num NumTest to 5 /*Here har vi vores global var */
dcl bool BoolTest
dcl text TextTest
dcl someClass ClassTest

Main begin 
   dcl num NumTest to 5 
   dcl bool BoolTest
   dcl text TextTest
   dcl someClass ClassTest
end

class SomeClass begin
   dcl num NumTest to 5 
   dcl bool BoolTest
   dcl text TextTest
   dcl someClass ClassTest

   dcl func x() begin
      dcl num NumTest to 5 
      dcl bool BoolTest
      dcl text TextTest
      dcl someClass ClassTest
      dcl num i to 1

      set NumTest to 5
      set BoolTest to true
      set TextTest to "test"
      set ClassTest to new someClass()

      if point.y > ball.point.y then /* hvorfor virker dette */
      end
      if NumTest > 6 then
      end

      for i upto 6 do
         for i upto 6 do
            if point.y > ball.point.y then /* hvorfor virker dette */
            end
         end
      end
      
      while i < 6 do
         while true do
            if NumTest = 6 then
            else
            end
         end
      end
   end
end